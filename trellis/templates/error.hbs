<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{status_code}} · {{error}} · studium.dev</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

  </head>
  <body>
    <section class="panel">
      <p
        class="pill"
        style="display:inline-flex;align-items:center;gap:8px;padding:6px 10px;border-radius:999px;background:#0e121f;border:1px solid var(--border);color:#f59e0b;font-size:12px;letter-spacing:0.3px;"
      >Server-side error</p>
      <h1>{{status_code}} · {{error}}</h1>
      <p>Something went off the rails. The backend returned the message above.</p>
      <p><code>curl -i https://studium.dev/api/health</code></p>
      <div class="actions">
        <a class="primary" href="/">Back to safety</a>
        <a href="/api/health">Check health</a>
        <a href="/feed">Open feed</a>
      </div>

      <footer>
        <p>
          Created with
          <a href="https://trellis.studium.dev/">Trellis v{{footer.version}}</a>
          ©
          {{footer.year}}
        </p>
        {{#if footer.links}}
          <ul>
            {{#each footer.links}}
              <li><a href="{{href}}">{{text}}</a></li>
            {{/each}}
          </ul>
        {{/if}}
      </footer>
    </section>
  </body>
</html>